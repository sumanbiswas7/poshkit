export const btnSizeMap = {
    sm: { fontSize: 12, height: 30, width: 30 },
    md: { fontSize: 14, height: 40, width: 40 },
    lg: { fontSize: 15, height: 50, width: 50 },
};

export interface BtnSizeMap {
    sm: {
        fontSize: number;
        height: number;
        width: number;
    };
    md: {
        fontSize: number;
        height: number;
        width: number;
    };
    lg: {
        fontSize: number;
        height: number;
        width: number;
    };
}